extends ./_layout

block content
  .row
    .onLoggedOut.hidden
      h1 Login
      form
        input(type="email", placeholder="email", required)
        input(type="password", placeholder="password", required)
        input.doRegister(type="button", value="Register")
        input(type="submit", value="Login")
        input.doResetPassword(type="button", value="Reset Password")
    .onTempPassword.hidden
      h1 Change Password
      form
        input(type="password", placeholder="Current Password")
        input(type="password", placeholder="New Password")
        input(type="submit", value="Change Password")
    .onLoggedIn
      h1 Hello
      button.doLogout Logout
  .row
    h1 Movie Watchlist
    .search
      input(type="text", placeholder="Enter movie title")
      button.myButton3 Search
    .searchResults
  .watchlist
